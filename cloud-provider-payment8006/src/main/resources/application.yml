server:
  port: 8006

spring:
  application:
    name: CLOUD-PAYMENT-SERVICE
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
  main:
    banner-mode: off